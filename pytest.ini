[pytest]
testpaths = tests
addopts =
    --strict-markers
    --verbose
    --tb=short
markers =
    slow: bakes a project AND installs + runs its generated test suite in an isolated venv (network access, slower)
